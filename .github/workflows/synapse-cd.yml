# =============================================================================
# synapse-cd.yml
# =============================================================================
#
# TWO DEPLOYMENTS, IN ORDER, EVERY TIME.
#
#   1. Workspace artifacts  src/synapse/workspace  -> azure.synapse.tools
#                            Linked services, SQL script tabs, notebooks.
#                            Deployed through the Dev REST API at
#                            <workspace>.dev.azuresynapse.net.
#
#   2. Serverless SQL OBJECTS src/synapse/serverless -> Invoke-Sqlcmd over TDS
#                            The edw_lake database, external data sources,
#                            file formats, views, procedures, grants.
#
# Step 2 is not optional and is not covered by step 1. This is the single most
# common misunderstanding about deploying Synapse: a "SQL script" artifact is a
# saved query TAB in Studio. Deploying it does not execute it. If you only run
# step 1, the workspace looks perfect in Studio and every ADF pipeline fails,
# because edw_lake does not exist.
#
# BOTH steps require the private data plane, which is why this cannot run on a
# GitHub-hosted runner:
#   - the Dev API resolves through privatelink.dev.azuresynapse.net
#   - the serverless endpoint resolves through privatelink.sql.azuresynapse.net
# =============================================================================

name: Synapse CD

on:
  push:
    branches: [main]
    paths:
      - 'src/synapse/**'
      - 'scripts/Deploy-Synapse.ps1'
      - 'scripts/Deploy-ServerlessSql.ps1'
      - 'scripts/New-DeploymentConfig.ps1'
      - '.github/workflows/synapse-cd.yml'
      - '.github/workflows/_synapse-deploy.yml'

  workflow_dispatch:
    inputs:
      environment:
        required: true
        default: 'all'
        type: choice
        options: [all, dev, test, prod]
      skip_artifacts:
        description: 'Skip workspace artifacts, deploy serverless SQL only'
        required: false
        default: false
        type: boolean
      skip_serverless:
        description: 'Skip serverless SQL, deploy workspace artifacts only'
        required: false
        default: false
        type: boolean

permissions:
  id-token: write
  contents: read

concurrency:
  group: synapse-cd
  cancel-in-progress: false

# There are deliberately no TERRAFORM_VERSION / SYNAPSE_TOOLS_VERSION variables
# here. Every deployment job below is a call to _synapse-deploy.yml, and the
# `env` context is NOT available in a reusable workflow's `with:` block - GitHub
# allows only github, needs, strategy, matrix, inputs and vars there. Version
# pins set at this level could never be passed down; they would sit in the file
# looking authoritative while the callee quietly used its own defaults. Both
# pins live on the inputs of _synapse-deploy.yml, which is also where to
# override them.

jobs:

  # ---------------------------------------------------------------------------
  # Which environments actually exist?
  #
  # test and prod have no infrastructure until infra-cd has run for them, and
  # every deployment job aimed at a missing environment fails on its first
  # Terraform read - so a normal push to main goes green on dev and then reports
  # two failures for environments nobody has created yet. That trains people to
  # ignore red, which is worse than the missing environments.
  #
  # This runs as the READ-ONLY CI identity: it needs no `environment:` block,
  # and its OIDC subject on main is repo:<owner>/<repo>:ref:refs/heads/main,
  # which bootstrap federates for exactly this kind of job.
  #
  # It FAILS OPEN. An environment is reported missing only on positive evidence
  # - the backend initialised and the state holds zero resources. If the check
  # itself cannot run, every environment is reported present and the deployment
  # jobs behave exactly as they did before this job existed. A broken probe must
  # never be able to silently skip a production deployment.
  # ---------------------------------------------------------------------------
  preflight:
    name: Which environments exist?
    runs-on: ${{ fromJSON(vars.RUNNER_LABELS || '["self-hosted","Linux","X64","edw"]') }}
    timeout-minutes: 10

    outputs:
      dev:  ${{ steps.check.outputs.dev }}
      test: ${{ steps.check.outputs.test }}
      prod: ${{ steps.check.outputs.prod }}

    env:
      ARM_USE_OIDC: 'true'
      ARM_CLIENT_ID: ${{ vars.AZURE_CI_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.9.8'
          terraform_wrapper: false

      - uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CI_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
        # A probe that cannot log in must not be able to skip a deployment.
        # Failing here leaves every output unset, which the gates below read as
        # "assume it exists".
        continue-on-error: true

      - name: Read each environment's state
        id: check
        working-directory: infra/terraform
        run: |
          set -uo pipefail
          for env in dev test prod; do
            deployed=true
            reason='probe failed - assuming it exists'
            if terraform init -reconfigure -no-color -backend-config="envs/$env/backend.hcl" >/dev/null 2>&1; then
              count=$(terraform state list 2>/dev/null | wc -l | tr -d ' ')
              if [ "$count" = "0" ]; then
                deployed=false
                reason='no resources in state - not deployed yet'
              else
                reason="$count resource(s) in state"
              fi
            fi
            echo "$env=$deployed" >> "$GITHUB_OUTPUT"
            printf '%-5s deployed=%-5s (%s)\n' "$env" "$deployed" "$reason"
          done

      - name: Summary
        run: |
          {
            echo '## Environments'
            echo
            echo '| Environment | Deployed |'
            echo '|---|---|'
            echo '| dev | ${{ steps.check.outputs.dev }} |'
            echo '| test | ${{ steps.check.outputs.test }} |'
            echo '| prod | ${{ steps.check.outputs.prod }} |'
            echo
            echo 'Jobs for an environment that does not exist are skipped, not failed.'
          } >> "$GITHUB_STEP_SUMMARY"

  validate:
    name: Validate
    runs-on: ${{ fromJSON(vars.RUNNER_LABELS || '["self-hosted","Linux","X64","edw"]') }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Validate artifact JSON
        shell: pwsh
        run: |
          $bad = 0
          Get-ChildItem src/synapse/workspace -Filter *.json -Recurse | ForEach-Object {
            try { $null = Get-Content $_.FullName -Raw | ConvertFrom-Json }
            catch {
              Write-Host "::error file=$($_.FullName)::$($_.Exception.Message)"
              $bad++
            }
          }
          if ($bad) { exit 1 }

      # The numeric prefixes ARE the dependency graph, and a gap or a duplicate
      # means something will run before its dependency exists. Cheap to check,
      # extremely annoying to debug.
      - name: Check serverless script ordering
        shell: pwsh
        run: |
          $scripts = Get-ChildItem src/synapse/serverless -Filter *.sql -File | Sort-Object Name

          $unprefixed = $scripts | Where-Object { $_.Name -notmatch '^\d{3}_' }
          if ($unprefixed) {
            Write-Host "::error::Every serverless script needs a NNN_ prefix - it is the execution order. Offenders: $($unprefixed.Name -join ', ')"
            exit 1
          }

          $prefixes = $scripts | ForEach-Object { $_.Name.Substring(0, 3) }
          $dupes = $prefixes | Group-Object | Where-Object Count -gt 1
          if ($dupes) {
            Write-Host "::error::Duplicate order prefixes: $($dupes.Name -join ', '). Execution order would be non-deterministic."
            exit 1
          }

          Write-Host "Serverless deployment order:"
          $scripts | ForEach-Object { Write-Host "  $($_.Name)" }

  dev:
    name: Deploy to dev
    needs: [validate, preflight]
    if: |
      needs.preflight.outputs.dev != 'false' &&
      (
        github.event_name == 'push' ||
        inputs.environment == 'all' ||
        inputs.environment == 'dev'
      )
    uses: ./.github/workflows/_synapse-deploy.yml
    with:
      environment: dev
      skip_artifacts: ${{ inputs.skip_artifacts == true }}
      skip_serverless: ${{ inputs.skip_serverless == true }}
    secrets: inherit

  test:
    name: Deploy to test
    needs: [dev, preflight]
    # `skipped` is accepted ONLY when the operator explicitly targeted this
    # environment alone. Accepting it unconditionally is what let prod deploy
    # after dev FAILED: dev failed -> test skipped -> prod saw "skipped" and
    # ran. 45 resources were created in production off a broken commit.
    #
    # Push and "all" therefore require the previous stage to have SUCCEEDED;
    # a single-environment dispatch requires the earlier stages to have been
    # skipped, which only happens when they were never selected.
    if: |
      always() &&
      needs.preflight.outputs.test != 'false' &&
      (
        ((github.event_name == 'push' || inputs.environment == 'all') &&
          needs.dev.result == 'success')
        ||
        (inputs.environment == 'test' && needs.dev.result == 'skipped')
      )
    uses: ./.github/workflows/_synapse-deploy.yml
    with:
      environment: test
      skip_artifacts: ${{ inputs.skip_artifacts == true }}
      skip_serverless: ${{ inputs.skip_serverless == true }}
    secrets: inherit

  prod:
    name: Deploy to prod
    needs: [dev, test, preflight]
    # `skipped` is accepted ONLY when the operator explicitly targeted this
    # environment alone. Accepting it unconditionally is what let prod deploy
    # after dev FAILED: dev failed -> test skipped -> prod saw "skipped" and
    # ran. 45 resources were created in production off a broken commit.
    #
    # Push and "all" therefore require the previous stage to have SUCCEEDED;
    # a single-environment dispatch requires the earlier stages to have been
    # skipped, which only happens when they were never selected.
    if: |
      always() &&
      needs.preflight.outputs.prod != 'false' &&
      (
        ((github.event_name == 'push' || inputs.environment == 'all') &&
          needs.dev.result == 'success' && needs.test.result == 'success')
        ||
        (inputs.environment == 'prod' &&
          needs.dev.result == 'skipped' && needs.test.result == 'skipped')
      )
    uses: ./.github/workflows/_synapse-deploy.yml
    with:
      environment: prod
      skip_artifacts: ${{ inputs.skip_artifacts == true }}
      skip_serverless: ${{ inputs.skip_serverless == true }}
    secrets: inherit
